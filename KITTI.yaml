# Ultralytics YOLO 🚀, AGPL-3.0 license
# COCO128 dataset https://www.kaggle.com/ultralytics/coco128 (first 128 images from COCO train2017) by Ultralytics
# Example usage: yolo train data=coco128.yaml
# parent
# ├── ultralytics
# └── datasets
#     └── coco128  ← downloads here (7 MB)


# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
train: E:\data\KITTI_TRACKING\training_yolotrack_format\data\train.txt  # train images (relative to 'path') 128 images
val: E:\data\KITTI_TRACKING\training_yolotrack_format\data\val.txt # val images (relative to 'path') 128 images
test:  # test images (optional)

# Classes
names:
  0: Cyclist
  1: Van
  2: Car
  3: Pedestrian
  4: Truck

